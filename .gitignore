node_modules
.codex
.env
.env.test
.env.test.local
dist
coverage

# Locally-stored uploads (dev disk storage; prod uses object storage)
uploads/

# Keep the S3-backed upload module in source control.
!src/modules/uploads/
!src/modules/uploads/**

# Runtime logs
*.log

# Claude Code local config
.claude/
.claude.zip
.playwright-mcp/
